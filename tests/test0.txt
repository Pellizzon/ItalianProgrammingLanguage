funzione fib(n) {
    se (n <= 1)
        ritorna n; 
    ritorna fib(n-1) + fib(n-2);
}

funzione teste(){
    stampa(leggere());
}

funzione main()
{
    var x = 10000;
    teste();
    stampa(teste());
    var x = fib(9);
    stampa(x);

    per (var i = 0; i < 10; i = i+ 1)
        stampa(fib(i));
}
